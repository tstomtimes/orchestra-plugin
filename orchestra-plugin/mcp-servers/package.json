{
  "name": "orchestra-plugin-mcp-servers",
  "version": "1.0.0",
  "description": "MCP servers for Orchestra Plugin",
  "main": "index.js",
  "scripts": {
    "browser": "ts-node browser-server.ts",
    "browser:dev": "nodemon --exec ts-node browser-server.ts",
    "install-browser": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
